module RecordsDemo exposing (Point, origin, moveRight)

import Lune.Int as Int

type alias Point =
  { x : Int
  , y : Int
  }

origin : Point
origin =
  { x = 0, y = 0 }

moveRight : Int -> Point -> Point
moveRight dx p =
  { p | x = Int.add p.x dx }
