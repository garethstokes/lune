module RecordsDemo exposing (Point, origin, moveRight)

import Lune.Prelude exposing (Int, addInt)

type alias Point =
  { x : Int
  , y : Int
  }

origin : Point
origin =
  { x = 0, y = 0 }

moveRight : Int -> Point -> Point
moveRight dx p =
  { p | x = addInt p.x dx }
