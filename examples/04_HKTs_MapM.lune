module HktDemo exposing (mapM)

import Lune.Prelude exposing (List(..), Monad(..))

mapM : Monad m => (a -> m b) -> List a -> m (List b)
mapM f xs =
  case xs of
    [] -> pure []
    Cons x xt ->
      do
        y  <- f x
        yt <- mapM f xt
        pure (Cons y yt)
