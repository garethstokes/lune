module FileRead exposing (main)

import Lune.IO as IO
import Lune.String as Str
import Lune.Prelude exposing (Result(..))

main : IO Unit
main =
  do
    result <- IO.readFile "README.md"
    case result of
      Ok _ ->
        IO.println "Successfully read README.md"
      Err _ ->
        IO.println "Error reading file"
