module FileRead exposing (main)

import Lune.IO as IO
import Lune.Task as Task

main : Task Unit Unit
main =
  Task.onError
    (do
      _ <- IO.readFile "README.md"
      IO.println "Successfully read README.md"
    )
    (\_ -> IO.println "Error reading file")
