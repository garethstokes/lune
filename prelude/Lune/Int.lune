module Lune.Int exposing (add, sub, mul, eq, gte, lte)

{-| Surface API per `spec/lune_standard_library_v0_1.md`. -}

import Lune.Prelude exposing (Bool(..), Int)

add : Int -> Int -> Int
add = prim_addInt

sub : Int -> Int -> Int
sub = prim_subInt

mul : Int -> Int -> Int
mul = prim_mulInt

eq : Int -> Int -> Bool
eq = prim_eqInt

gte : Int -> Int -> Bool
gte = prim_geInt

lte : Int -> Int -> Bool
lte = prim_leInt
